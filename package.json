{
  "name": "3d-game-template",
  "version": "1.0.0",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && npx http-server ."
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/three": "^0.150.0"
  },
  "dependencies": {
    "three": "^0.150.0"
  }
}
